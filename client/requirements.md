## Packages
framer-motion | For smooth animations of the consent banner and ad transitions

## Notes
- Ad iframe URLs must be constructed exactly as per the legacy implementation
- Cookie consent state is stored in localStorage ('publisherCookieConsent')
- Consent strings are imported from @shared/schema
- Google Fonts: 'Outfit' for headings, 'Inter' for body
